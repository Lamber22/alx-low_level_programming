#include "main.h"

/**
 * _isupper - a function that checks for uppercase character
 * @c: the character to print
 * Return: 0
 */

int _isupper(int c)
{
	if (c = 'A')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
