#include "main.h"

/**
 * _isalpha - checking for alphabetic character
 * @c: printing alpha characters
 * Return: 0 (Success)
 */

int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z' (c++)) || (c >= 'A' && c <= 'Z' (c++)))
		return (1);
	else
		return (0);
}
