#include "main.h"

/**
 * add - a program that adds two intergers and return the result
 * @one: first number input
 * @two: second number input
 * Return: one + two
 */

int add(int one, int two)
{
	return (one + two);
}
